version: "3.8"

services:
  stream_bot:
    build: .
    container_name: tg_stream_bot
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
